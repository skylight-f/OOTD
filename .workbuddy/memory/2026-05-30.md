# 2026-05-30

## TypeScript 全面重构

### 1. 修复 Mpx 组件生命周期错误（onLoad → attached）
- 4 个组件误用 `onLoad`（Page 专属钩子），改为 `attached` + `pageLifetimes.show`
- 详情页用 `getCurrentPages()` 获取 URL 参数

### 2. wx API 类型声明（项目内手写，不引外部包）
- 在 `global.d.ts` 手写项目实际用到的 wx API 类型（导航/交互/网络/媒体/位置/存储/文件系统/环境变量）
- 不引入 `miniprogram-api-typings`，用户明确要求修 TS 问题不靠加包解决
- `weather.ts` 中使用 `wx.RequestSuccessCallbackResult`（来自 global.d.ts 的 namespace 声明）

### 3. 移除 methods 中的 `this: any`
- 47 处 `methods` 内的 `this: any` 全部移除（Mpx `ThisType` 自动推断）
- 18 处生命周期钩子（`attached`/`pageLifetimes.show`/`onShow`）保留 `this: any`（框架类型限制）

### 4. 修复运行时 bug
- `MyPage.mpx:85` — `Set.add()` 不支持 spread 参数
- `ClosetDashboardPage.mpx:103` — `.length` 误用在 `number` 上
- `ClosetPage` / `AddClothesPage` — 重复定义 `detectTone`/`categoryToIcon`，统一改为导入 `storage.ts`

### 5. 构建验证
- `npx mpx-cli-service build` → compiled successfully，零 TS 错误
