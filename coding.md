# 编码规范
1. 不要修改后执行 build 命令
2. 页面组件命名使用 PascalCase, 组件文件名使用 PascalCase，文件命名使用 pascalCase
3. 页面文件夹结构：
   ```
   pages/
     myPage/
       component
          Component.mpx
       index.mpx
   ```
4. 样式预处理器统一使用 less，项目内不要再使用 stylus